class Sample

functions

SampleFunction : int*nat*nat1 -> seq of char
    SampleFunction(a, b, c) ==
       if(a <= 10) then
         if(-1 < a) then
      	   "aは-1より大きく10以下です"
    	 elseif(b > 0) then
    	   "aは-1以下で、bは0より大きいです"
	     else
	       "aは-1以下で、bは0以下です"
       elseif(b >= -10) then
         if( b <= -3 ) then
           "aは10より大きく、bは-3以下です"
         elseif(a < 20) then
           "aは10より大きく20未満で、bは-3より大きいです"
         else
	       "aは20以上で、bは-3より大きいです"
	   else
	     if(c >= 4)then
	       "cは4よりでかい"
	     else
	       "cは4よりでかくない";
end Sample

