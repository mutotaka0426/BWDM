class hoge

functions

	public func : nat * nat -> seq of char
		func(a, b) ==
			if(a + b <= 50) then
				if(a >= 18) then
					if( b >= 16) then
						"へい"
					else
     					"へい！"
				else
					"へいへいへい"
			else
				"other";

end hoge

