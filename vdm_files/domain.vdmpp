class 遊園地チケット

functions

static public 割引判定 : int * int -> seq of char
	割引判定 (夫の年齢, 妻の年齢) ==
		if(夫の年齢 + 妻の年齢 <= 50) then
			if(夫の年齢 >= 18) then
				if(妻の年齢 >= 16) then
					"割引価格となる"
				else
					"割引価格とならない (妻の年齢 < 16)"
			else
				"割引価格とならない (夫の年齢 < 18)"
		else
			"割引価格とならない (夫の年齢 + 妻の年齢 > 50)";

end 遊園地チケット
